<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">
   <group android:id="@+id/g1">
    <item android:title="Maths Functions"
        android:id="@+id/trigno"
        android:icon="@drawable/trigno"
        app:showAsAction="never"/>
   </group>

    <group android:id="@+id/g2">
    <item android:title="Unit Converter"
        android:id="@+id/temp"
        android:icon="@drawable/unit"
        app:showAsAction="never"/>

    </group>

    <group android:id="@+id/g3">
    <item android:title="History"
        android:icon="@drawable/history"
        android:id="@+id/history"
        app:showAsAction="never"/>

    </group>

    <group android:id="@+id/g4">
    <item android:title="About"
        android:icon="@drawable/aboutapp"
        android:id="@+id/about"
        app:showAsAction="never"/>
    </group>
</menu>